<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Отчёт CORS-сканера</title>
    <style>
        body { font-family: Arial; background: #f4f4f4; padding: 20px; }
        h1, h2 { color: #333; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ccc; padding: 8px; }
        th { background: #eee; }
        .missing { color: red; font-weight: bold; }
        .ok { color: green; font-weight: bold; }
        .warning { color: orange; }
    </style>
</head>
<body>
    <h1>🔐 Отчёт CORS-сканера</h1>
    <p><strong>Цель:</strong> {{ target }}</p>
    <p><strong>Дата:</strong> {{ date }}</p>

    <h2>Заголовки безопасности</h2>
    <table>
        <tr><th>Заголовок</th><th>Статус</th><th>Риск</th></tr>
        {% for h in headers %}
        <tr>
            <td>{{ h.name }}</td>
            <td class="{{ 'missing' if not h.present else 'ok' }}">
                {{ '❌ Отсутствует' if not h.present else '✅ Присутствует' }}
            </td>
            <td>{{ h.risk }}</td>
        </tr>
        {% endfor %}
    </table>

    <h2>⚠️ Выявленные проблемы</h2>
    <ul>
        {% for issue in issues %}
        <li class="warning">❌ {{ issue }}</li>
        {% endfor %}
    </ul>
</body>
</html>
